# GA-PubSub Core Demo — Frontend
# Copy to .env.local

# URL of the backend when running locally.
# Leave empty to run in fully in-browser mode (no backend required).
VITE_API_URL=http://localhost:3001

# Set by CI for GitHub Pages. Leave blank for local dev.
# VITE_BASE_URL=/ga-pubsub/
