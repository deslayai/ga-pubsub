// PRO-only tests — live in the ga-pubsub-pro private repository
// Skipped in public repo.

import { describe, it } from 'vitest';
describe.skip('PRO tests (private repo only)', () => { it('placeholder', () => {}) });
